# Handle line endings automatically for text files
# and force them to LF on check-in/check-out
* text=auto eol=lf

# Explicitly force IDE files to LF
*.iml text eol=lf

# Ensure Salesforce metadata and code are always LF
*.cls text eol=lf
*.trigger text eol=lf
*.xml text eol=lf
